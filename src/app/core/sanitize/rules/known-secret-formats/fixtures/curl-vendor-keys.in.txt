curl https://api.github.com/repos/example/orders/actions/runs -H 'Authorization: token {{fake:github-token}}'
curl https://api.stripe.com/v1/charges -u {{fake:stripe-key}}: -d amount=2000 -d currency=eur
curl 'https://maps.googleapis.com/maps/api/geocode/json?address=Berlin&key={{fake:google-api-key}}'
curl -X POST -H 'Content-type: application/json' --data '{"text":"deploy done"}' https://hooks.slack.com/services/{{fake:slack-webhook}}
